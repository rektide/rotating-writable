{
	"name": "rotating-writable",
	"version": "1.0.0",
	"description": "A rolling fs writable stream for writing log files into, where a date suffix gets appended to the filename",
	"main": "rotating-writable.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "https://archive.voodoowarez.com/rotating-writable"
	},
	"keywords": [],
	"author": "rektide de la faye",
	"license": "MIT",
	"dependencies": {
		"hourly": "^1.0.0"
	}
}
